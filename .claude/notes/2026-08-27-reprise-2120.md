# Reprise (dernière mise à jour : 2026-08-29, 11 h 20)

## État
- **Chantier 1 fusionné dans `main`** (`eecbbea`, 27/08) : `/erom-seo:audit` niveau 0, 27 vérifications, recette AC-1 à AC-7.
- **Correctif apex/www fusionné le 28/08** (`a9dbba0`, `8d43a49`) : un sitemap listé en apex sur un site en www vidait la collecte en silence. `sameSite`, stats `sitemapUrls`, avertissement stderr, `--max-pages N` = N pages.
- **Chantier 2 fusionné dans `main` le 28/08** (`eb5c6ce`, merge de `chantier-2-strategy`, 15 commits) : `/erom-seo:strategy` (interview, `seo/strategy.md` strict, `keywords.ts` Bing + Wikimedia, `lint-strategy.ts`), couche stratégique de l'audit (STRAT-01 à 04, AI-02, `strategy-eval.ts`, active dès que `seo/strategy.md` existe, D9), niveau 2 sur localhost (sitemap de prod ramené en local, PageSpeed et sondes d'hôte non applicables). 170 tests, 57 citations officielles retrouvées. Exécuté par la session Sonnet `claude-flora-ol5a` en subagent-driven, revu sur le diff complet par Fable avant fusion (rien de bloquant).
- Recette : `docs/superpowers/plans/2026-08-28-erom-seo-chantier-2-recette.md`, AC-1 à AC-8 tous courus. AC-1 par Romain avec un autre modèle (GLM-5.3-flash) : la skill a tenu, bon signe de robustesse.
- Cobaye : `/Users/recarnot/dev/chico-happiness` (commentchercherbonheur.org, site parodique de Romain, Next.js 16). Y restent, non commités, au choix de Romain : `seo/strategy.md` + `seo/strategy/2026-08-28/` + `seo/audits/…` (niveau 0 et niveau 2), et le diff `src/app/methode/` (title + h1 corrigés pour AC-5, plus un `layout.tsx` minimal qui porte le title).
- **Ménage parqué du chantier 2 fusionné le 28/08 à 12 h 55** (`a38b18e`, 7 commits de la session Sonnet `claude-mercure-0im1`, worktree `erom-agence-seo-menage`) : test `declaredLocal`, `--strategy-path <fichier absent>` = erreur consignée (« fichier absent ») au lieu d'un silence, le lint contrôle le nombre de vérifications de l'en-tête, `strategy-eval.ts` rend une erreur lisible (exit 1), `jsonLdBlocks` retiré, STRAT-01 sans phrase en double, spec AC-3 et AC-6 comptent les ids. 178 tests, 57 citations.
- Rien n'est poussé.

## Chantier 3, le verbe `build` : tâches 1 à 6 fusionnées le 28/08 à 18 h 50, tâche 7 courue le 29/08
- Spec `docs/superpowers/specs/2026-08-28-erom-seo-build-design.md` (D16 à D22, AC-1 à AC-8) ; plan `docs/superpowers/plans/2026-08-28-erom-seo-chantier-3-build.md` (7 tâches) ; recherche `docs/recherches/2026-08-28-nextjs-16-seo-api.md` ; recette `docs/superpowers/plans/2026-08-28-erom-seo-chantier-3-recette.md` (AC-1 et tests OK, AC-2 à AC-8 avec la procédure à dérouler).
- Décisions de Romain : textes visibles (h1, première phrase) via une table validée avant toute modification, puis build autonome (D16) ; build lance le serveur local, l'audit niveau 2, et boucle (2 passages max, D17) ; un script `plan.ts` + la skill (D18).
- Livré : `plugin/lib/report.ts` (parseReport, latestAuditDir, sortAuditDirs), `skills/build/` (SKILL.md six temps, `scripts/plan.ts`, `lib/plan.ts` avec KINDS, `lib/recipes.ts`, `references/nextjs.md` 13 recettes, `references/autre-stack.md`), `check-sources.ts` étendu (80 citations). 209 tests. Exécuté par `claude-mercure-0im1` (9 commits), relu et fusionné par Fable. Incident : deux reviewers sous-agents muets pendant l'exécution (3 h perdues avant le premier ping), règle des 10 minutes posée depuis, voir la mémoire `subagent-reviewer-muet`.
- **Tâche 7 courue le 29/08** (session chico `chico-happiness-49`, textes validés « ok sur tout » par Romain, AC-8 joué en headless par Fable sur une branche jetable). Résultat dans la recette, section « Résultats du 2026-08-29 » : OK AC-1 à AC-5 et AC-8 ; **KO AC-6** (R-6 : le plan part de l'audit localhost du 28/08, le hors build IDX-04 de l'audit prod disparaît en silence, `latestAuditDir` départage n0 et n2 du même jour par mtime) ; **KO partiel AC-7** (R-7a : phrases d'ouverture dupliquées sur `/ascension` et `/methode`, la règle « ne rien supprimer » lue au pied de la lettre ; R-7b : ouverture de la home et lignes « Mis à jour le » hors table validée, annoncées par build). Mineurs R-3, R-4, R-7c, R-7d, R-8 listés au bilan de la recette.
- **R-7a et R-6 réglés le 29/08 à 12 h** (décision de Romain « 1 oui, 2a », exécution par la session chico `chico-happiness-49` dans le worktree `erom-agence-seo-recette-fix`, revue et fusion Fable `66f7378`) : règle texte de la skill « remplacer », `plan.ts` rapatrie les hors build du dernier audit n0 avec `origine`, rapport n0 illisible signalé sur stderr, 216 tests. Détail dans la recette, section « Correctifs du 2026-08-29 ».
- **Chico au 29/08, 11 h 45** : branche `seo-build-2026-08-29` fusionnée dans `main` de chico (`059480b`, décision de Romain) : 8 commits `seo(...)`, le rapport `2026-08-29-n2-3` à `0 Critique · 0 Important · 0 Mineur · 1 Info`, `seo/**/raw/` hors git, phrases dupliquées corrigées. Rien poussé (le push est une décision de Romain). Restent : IDX-04 à régler sur Vercel (Domains, Redirect to), R-7b (mineur), suppression des worktrees fusionnés `erom-agence-seo-chantier-3`, `erom-agence-seo-menage`, `erom-agence-seo-recette-fix` et de leurs branches, sur décision de Romain. Ensuite : chantier 4, `launch`.
- Dette parquée par la revue finale (mineure, chantier 4) : une page prévue absente du site est un warning du plan, pas une trouvaille hors build formelle ; `--audit` sans valeur est silencieux ; `package.json` lu deux fois dans `plan.ts`.

## Parqué, non bloquant, à reprendre quand un chantier passe à côté
- Chantier 1 : IDX-04 ne dit rien du cas 307 (précision à écrire dans `checks/indexability.md`) ; la preuve « fichier + lignes » ne marche pas sur un HTML minifié (le template pourrait dire « champ derived + grep »).
- Chantier 2 : au niveau 2 un hôte tiers listé dans le sitemap est réécrit comme l'hôte de prod (tradeoff D14, décision de Romain si ça gêne un jour). Le reste de la liste parquée a été traité par le ménage du 28/08 (voir État).
- Worktrees `erom-agence-seo-fix` et `erom-agence-seo-chantier-2` et leurs branches : supprimés le 28/08 à 11 h 30 après fusion, sur décision de Romain. Worktree `erom-agence-seo-menage` et branche `menage-chantier-2` : fusionnés à 12 h 55, encore présents, à supprimer sur décision de Romain (`git worktree remove /Users/recarnot/dev/erom-agence-seo-menage && git branch -d menage-chantier-2`). Branche `chantier-1-audit-n0` conservée depuis le chantier 1. Pour le chantier 3 : un nouveau worktree.

## Sécurité et calendrier
- **Incident du 28/08** : la clé `BING_WMT_API_KEY` affichée en clair dans le chat d'une session pendant la recette (commande de vérification mal construite). Romain a décidé le 28/08 à 11 h 57 de la régénérer lui-même (Bing Webmaster Tools, Settings, API Access ; une seule clé par compte, l'ancienne meurt) et de mettre `~/.zshenv` à jour. Si `keywords.ts` répond « clé refusée par Bing (InvalidApiKey) », c'est que `~/.zshenv` porte encore l'ancienne clé. Mémoire `erom-seo-secrets-env.md` mise à jour.
- **1er septembre** : rejouer la sonde JSON Bing (`docs/recherches/2026-08-27-mots-cles-gratuits.md`, section « Sondes reproductibles », attendu 400 InvalidApiKey) après le retrait SOAP/POX du 31 août ; `keywords.ts` dépend de cet endpoint (`BING_API_BASE`).

## Utilisation
- Depuis le dossier du site (`clients/<domaine>/` ou le repo du site) : `source ~/.zshenv` puis `claude --plugin-dir /Users/recarnot/dev/erom-agence-seo/plugin`, puis `/erom-seo:strategy` ou `/erom-seo:audit <url>` (`http://localhost:3000` pour le niveau 2).
- Les skills se chargent au démarrage de la session : relancer `claude` après une modification du plugin.

## Hors chantier
- Règle anti-fuite de secrets dans le garde : confiée par Romain à une autre session.
- `.claude/skills/plugin-release/` et `.claude/skillsTMP/` : dépôts de Romain, à ignorer.
