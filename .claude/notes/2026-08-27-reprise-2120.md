# Reprise (dernière mise à jour : 2026-08-29, 22 h)

## Chantier 4, le verbe `checklist` (ex `launch`) : spec, recherche et plan prêts le 29/08, exécution à lancer
- Décisions de Romain le 29/08 (13 h 16 à 14 h 08) : le verbe s'appelle `checklist`, il ne déploie ni ne lance rien (« On ne launch que dalle », D23) ; l'audit juge, un script écrit `seo/checklist.md` (D24) ; deux moitiés, un fichier relançable, cases `auto` / `main` / `action` (D25) ; aucune écriture sans `--agir`, deux actions : ping IndexNow et `SubmitFeed` chez Bing, Google à la main par construction (D26) ; lignes fixes reconnues par libellé (D27) ; site repris en option, `--ancien-sitemap` (D28) ; suivi J+N dans le fichier seul, pas d'agenda.
- Spec `docs/superpowers/specs/2026-08-29-erom-seo-checklist-design.md` (`294bcd0`), amendée d'après la note niveau 1 de la session d'idéation (`docs/recherches/2026-08-29-niveau-1-apis.md`, commitée ici en `0b90ac8`). Recherche pré-plan `docs/recherches/2026-08-29-checklist-indexnow-bing-gsc.md` (`6cad42f`) : corps IndexNow, `SubmitFeed` et `GetUserSites` figés sur les exemples officiels, `GetUserSites` réel = `{"d":[]}` (le compte Bing de Romain n'a aucun site), clé IndexNow de chico servie en 200, 10 URL au sitemap.
- Plan `docs/superpowers/plans/2026-08-29-erom-seo-chantier-4-checklist.md` (`294bcd0`), 7 tâches : 1 à 4 = `lib/checklist.ts`, `lib/actions.ts`, `lib/ancien-sitemap.ts`, CLI `checklist.ts` (code exécuté avant l'écriture du plan, 37 tests verts dans le scratchpad) ; 5 = `references/consoles.md` + `check-sources.ts` (24 citations retrouvées) ; 6 = SKILL.md, README, manifeste, recette ; 7 = recette sur chico (session mère, AC-4 envoie un vrai ping IndexNow avec l'accord de Romain). Worktree prévu : `/Users/recarnot/dev/erom-agence-seo-chantier-4`, branche `chantier-4-checklist`.
- Leçon du jour : les agents `search-builtin` n'ont ni shell ni écriture de fichier et leurs rapports sont tronqués vers 3 000 caractères ; pour un brief documentaire, demander des messages courts par section, ou `curl` la page soi-même (c'est ce qui a réglé IndexNow et Bing en dix minutes). `support.google.com` répond 404 à un HEAD, 200 à un GET.
- **Poussé sur GitHub le 29/08 à 14 h 47** (`main` → `origin/main`, décision de Romain « Déjà Pousse sur Github puis subagent sonnet »).
- **Exécution du plan lancée le 29/08 à 14 h 50** en subagent-driven (Fable orchestre, implémenteurs et reviewers Sonnet), worktree `/Users/recarnot/dev/erom-agence-seo-chantier-4`, branche `chantier-4-checklist`. Registre : `<worktree>/.superpowers/sdd/2026-08-29-erom-seo-chantier-4-checklist/progress.md` (rulings, revues, mineurs différés ; à lire d'abord en cas de reprise). **Fusionné dans `main` à 16 h 25** (`6bca027`, 11 commits : tâches 1 à 6, deux fix rounds, vague de correctifs F1 à F10 après la revue finale Fable, re-revue propre ; 258 tests, 107 citations). **Recette sur chico (tâche 7) jouée à 17 h 35** : AC-7, AC-1, AC-2, AC-3, AC-5, AC-6 OK ; R-1 corrigé sur `main` (`aee1bf1`, 259 tests) ; R-2 broutille non corrigée (gabarit `<site>` dans la consigne J+1) ; **AC-4 OK à 17 h 45** sur accord de Romain (« lance --agir ») : premier essai vide (R-3, sitemap en apex et site en www, filtre F10 trop strict), corrigé sur `main` (`d554726`, `urlsOnOrigin`, 260 tests), second essai **IndexNow 202, 10 URL**, case cochée et gardée à la relance. **Chantier 4 terminé**, recette complète (`667ade9`). Ensuite : chantier 5 (niveau 1, APIs Search Console et Bing ; note `docs/recherches/2026-08-29-niveau-1-apis.md`). Résultats : `docs/superpowers/plans/2026-08-29-erom-seo-chantier-4-recette.md`. Incident : l'audit prod headless a été coupé une fois par la limite de session (17 h 30), relancé après le reset. Dans chico, non commités : `seo/checklist.md` et `seo/audits/2026-08-29-n0/` (rapport et derived ; raw hors git) : commit `seo(checklist): …` sur décision de Romain. Worktree et branche du chantier 4 supprimés à 17 h 52 ; `main` poussé (`89c9a28`) et chico poussé (`a6d83ff`) sur décision de Romain.

## Chantier 5 étape 1, le verbe `console` : fusionné et poussé le 29/08 au soir
- Décisions de Romain (18 h 06 à 18 h 40) : le verbe de consultation **avant** l'audit niveau 1 (D29) ; skill et scripts bun, **pas de MCP** (D31 hors périmètre, tranché à 18 h 20) ; trois commandes seulement, `sites`, `inspect <url>`, `crawl`, les requêtes réelles reportées à l'étape 2 ; auth Google à deux fournisseurs, **gcloud aujourd'hui avec une doc de bascule vers un compte de service** (D32) ; `BING_API_BASE` recopié dans la skill, mise en commun reportée après inventaire (D34).
- Spec `docs/superpowers/specs/2026-08-29-erom-seo-console-design.md` (D29 à D34, AC-1 à AC-10). Plan `docs/superpowers/plans/2026-08-29-erom-seo-chantier-5-console.md`, 8 tâches. Recette `docs/superpowers/plans/2026-08-29-erom-seo-chantier-5-recette.md`, **10 critères sur 10**. Journal des décisions `docs/superpowers/journaux/2026-08-29-erom-seo-chantier-5-journal.md`.
- **Livré** : `plugin/skills/console/` (CLI plus cinq modules), `references/acces.md` (ACC-01 à ACC-06, 8 citations officielles), `check-sources.ts` étendu. **375 tests, 115 citations, 0 échec.** Fusionné dans `main` et poussé (`cd01aad`) sur décision de Romain à 22 h. Worktree et branche supprimés dans la foulée, même décision.
- **Ce que la journée a appris** : le compte Bing de Romain, vide toute la journée, a été rempli par lui en cours de session. Les quatre méthodes Bing jamais capturables l'ont enfin été (spec, section 12.6), **l'incertitude 1 est levée** (une URL inconnue rend un objet à sentinelles `DateTime.MinValue`, pas `null` ; `HttpStatus` vaut 0 même sur une URL indexée), l'incertitude 2 est réduite (`GetCrawlStats` et `GetCrawlIssues` rendent `[]` sur des sites sans historique).
- **Résidus, tous documentés dans la recette**, aucun ne bloque : deux chaînes hors du filet anti-tiret cadratin, trois `*_UNSPECIFIED` de Google affichés en brut (la règle retenue serait de les omettre, pas de les traduire), un `RangeError` de date impossible en pratique, une branche morte. Environ vingt minutes à eux tous.
- **Prérequis posés par Romain** : `GSC_QUOTA_PROJECT="gen-lang-client-0479935649"` dans `~/.zshenv`, API Search Console activée sur ce projet, et ses deux sites ajoutés dans Bing Webmaster Tools.
- **Ensuite** : chantier 5 étape 2, l'audit niveau 1 (`collect.ts --level 1`, LVL1-01 à LVL1-05, AI-03, imports CSV des rapports IA), qui réutilise les cinq modules de l'étape 1. Note d'idéation `docs/recherches/2026-08-29-niveau-1-apis.md`, section 6.

## Reprise au 29/08, 11 h 20 (chantiers 1 à 3)

## État
- **Chantier 1 fusionné dans `main`** (`eecbbea`, 27/08) : `/erom-seo:audit` niveau 0, 27 vérifications, recette AC-1 à AC-7.
- **Correctif apex/www fusionné le 28/08** (`a9dbba0`, `8d43a49`) : un sitemap listé en apex sur un site en www vidait la collecte en silence. `sameSite`, stats `sitemapUrls`, avertissement stderr, `--max-pages N` = N pages.
- **Chantier 2 fusionné dans `main` le 28/08** (`eb5c6ce`, merge de `chantier-2-strategy`, 15 commits) : `/erom-seo:strategy` (interview, `seo/strategy.md` strict, `keywords.ts` Bing + Wikimedia, `lint-strategy.ts`), couche stratégique de l'audit (STRAT-01 à 04, AI-02, `strategy-eval.ts`, active dès que `seo/strategy.md` existe, D9), niveau 2 sur localhost (sitemap de prod ramené en local, PageSpeed et sondes d'hôte non applicables). 170 tests, 57 citations officielles retrouvées. Exécuté par la session Sonnet `claude-flora-ol5a` en subagent-driven, revu sur le diff complet par Fable avant fusion (rien de bloquant).
- Recette : `docs/superpowers/plans/2026-08-28-erom-seo-chantier-2-recette.md`, AC-1 à AC-8 tous courus. AC-1 par Romain avec un autre modèle (GLM-5.3-flash) : la skill a tenu, bon signe de robustesse.
- Cobaye : `/Users/recarnot/dev/chico-happiness` (commentchercherbonheur.org, site parodique de Romain, Next.js 16). Y restent, non commités, au choix de Romain : `seo/strategy.md` + `seo/strategy/2026-08-28/` + `seo/audits/…` (niveau 0 et niveau 2), et le diff `src/app/methode/` (title + h1 corrigés pour AC-5, plus un `layout.tsx` minimal qui porte le title).
- **Ménage parqué du chantier 2 fusionné le 28/08 à 12 h 55** (`a38b18e`, 7 commits de la session Sonnet `claude-mercure-0im1`, worktree `erom-agence-seo-menage`) : test `declaredLocal`, `--strategy-path <fichier absent>` = erreur consignée (« fichier absent ») au lieu d'un silence, le lint contrôle le nombre de vérifications de l'en-tête, `strategy-eval.ts` rend une erreur lisible (exit 1), `jsonLdBlocks` retiré, STRAT-01 sans phrase en double, spec AC-3 et AC-6 comptent les ids. 178 tests, 57 citations.
- Rien n'est poussé.

## Chantier 3, le verbe `build` : tâches 1 à 6 fusionnées le 28/08 à 18 h 50, tâche 7 courue le 29/08
- Spec `docs/superpowers/specs/2026-08-28-erom-seo-build-design.md` (D16 à D22, AC-1 à AC-8) ; plan `docs/superpowers/plans/2026-08-28-erom-seo-chantier-3-build.md` (7 tâches) ; recherche `docs/recherches/2026-08-28-nextjs-16-seo-api.md` ; recette `docs/superpowers/plans/2026-08-28-erom-seo-chantier-3-recette.md` (AC-1 et tests OK, AC-2 à AC-8 avec la procédure à dérouler).
- Décisions de Romain : textes visibles (h1, première phrase) via une table validée avant toute modification, puis build autonome (D16) ; build lance le serveur local, l'audit niveau 2, et boucle (2 passages max, D17) ; un script `plan.ts` + la skill (D18).
- Livré : `plugin/lib/report.ts` (parseReport, latestAuditDir, sortAuditDirs), `skills/build/` (SKILL.md six temps, `scripts/plan.ts`, `lib/plan.ts` avec KINDS, `lib/recipes.ts`, `references/nextjs.md` 13 recettes, `references/autre-stack.md`), `check-sources.ts` étendu (80 citations). 209 tests. Exécuté par `claude-mercure-0im1` (9 commits), relu et fusionné par Fable. Incident : deux reviewers sous-agents muets pendant l'exécution (3 h perdues avant le premier ping), règle des 10 minutes posée depuis, voir la mémoire `subagent-reviewer-muet`.
- **Tâche 7 courue le 29/08** (session chico `chico-happiness-49`, textes validés « ok sur tout » par Romain, AC-8 joué en headless par Fable sur une branche jetable). Résultat dans la recette, section « Résultats du 2026-08-29 » : OK AC-1 à AC-5 et AC-8 ; **KO AC-6** (R-6 : le plan part de l'audit localhost du 28/08, le hors build IDX-04 de l'audit prod disparaît en silence, `latestAuditDir` départage n0 et n2 du même jour par mtime) ; **KO partiel AC-7** (R-7a : phrases d'ouverture dupliquées sur `/ascension` et `/methode`, la règle « ne rien supprimer » lue au pied de la lettre ; R-7b : ouverture de la home et lignes « Mis à jour le » hors table validée, annoncées par build). Mineurs R-3, R-4, R-7c, R-7d, R-8 listés au bilan de la recette.
- **R-7a et R-6 réglés le 29/08 à 12 h** (décision de Romain « 1 oui, 2a », exécution par la session chico `chico-happiness-49` dans le worktree `erom-agence-seo-recette-fix`, revue et fusion Fable `66f7378`) : règle texte de la skill « remplacer », `plan.ts` rapatrie les hors build du dernier audit n0 avec `origine`, rapport n0 illisible signalé sur stderr, 216 tests. Détail dans la recette, section « Correctifs du 2026-08-29 ».
- **Chico au 29/08, 11 h 45** : branche `seo-build-2026-08-29` fusionnée dans `main` de chico (`059480b`, décision de Romain) : 8 commits `seo(...)`, le rapport `2026-08-29-n2-3` à `0 Critique · 0 Important · 0 Mineur · 1 Info`, `seo/**/raw/` hors git, phrases dupliquées corrigées. Poussé sur `origin/main` le 29/08 à 11 h 50 (décision de Romain). IDX-04 réglé par Romain dans Vercel (Settings, Domains, Edit sur l'apex, Redirect to www, code 308) : vérifié à 11 h 55, `curl -sI https://commentchercherbonheur.org/` rend `HTTP/2 308`, idem sur `/methode` et `/sitemap.xml`. Les trois worktrees fusionnés et leurs branches supprimés à 11 h 50 (décision de Romain) ; reste `chantier-1-audit-n0`. Reste R-7b (mineur) et les broutilles de la recette. Ensuite : chantier 4, `launch`, « plus tard » (Romain, 29/08).
- Dette parquée par la revue finale (mineure, chantier 4) : une page prévue absente du site est un warning du plan, pas une trouvaille hors build formelle ; `--audit` sans valeur est silencieux ; `package.json` lu deux fois dans `plan.ts`.

## Parqué, non bloquant, à reprendre quand un chantier passe à côté
- Chantier 1 : IDX-04 ne dit rien du cas 307 (précision à écrire dans `checks/indexability.md`) ; la preuve « fichier + lignes » ne marche pas sur un HTML minifié (le template pourrait dire « champ derived + grep »).
- Chantier 2 : au niveau 2 un hôte tiers listé dans le sitemap est réécrit comme l'hôte de prod (tradeoff D14, décision de Romain si ça gêne un jour). Le reste de la liste parquée a été traité par le ménage du 28/08 (voir État).
- Worktrees `erom-agence-seo-fix` et `erom-agence-seo-chantier-2` et leurs branches : supprimés le 28/08 à 11 h 30 après fusion, sur décision de Romain. Worktree `erom-agence-seo-menage` et branche `menage-chantier-2` : fusionnés à 12 h 55, encore présents, à supprimer sur décision de Romain (`git worktree remove /Users/recarnot/dev/erom-agence-seo-menage && git branch -d menage-chantier-2`). Branche `chantier-1-audit-n0` conservée depuis le chantier 1. Pour le chantier 3 : un nouveau worktree.

## Sécurité et calendrier
- **Incident du 28/08** : la clé `BING_WMT_API_KEY` affichée en clair dans le chat d'une session pendant la recette (commande de vérification mal construite). Romain a décidé le 28/08 à 11 h 57 de la régénérer lui-même (Bing Webmaster Tools, Settings, API Access ; une seule clé par compte, l'ancienne meurt) et de mettre `~/.zshenv` à jour. Si `keywords.ts` répond « clé refusée par Bing (InvalidApiKey) », c'est que `~/.zshenv` porte encore l'ancienne clé. Mémoire `erom-seo-secrets-env.md` mise à jour.
- **1er septembre** : rejouer la sonde JSON Bing (`docs/recherches/2026-08-27-mots-cles-gratuits.md`, section « Sondes reproductibles », attendu 400 InvalidApiKey) après le retrait SOAP/POX du 31 août ; `keywords.ts` dépend de cet endpoint (`BING_API_BASE`).

## Utilisation
- Depuis le dossier du site (`clients/<domaine>/` ou le repo du site) : `source ~/.zshenv` puis `claude --plugin-dir /Users/recarnot/dev/erom-agence-seo/plugin`, puis `/erom-seo:strategy` ou `/erom-seo:audit <url>` (`http://localhost:3000` pour le niveau 2).
- Les skills se chargent au démarrage de la session : relancer `claude` après une modification du plugin.

## Hors chantier
- Règle anti-fuite de secrets dans le garde : confiée par Romain à une autre session.
- `.claude/skills/plugin-release/` et `.claude/skillsTMP/` : dépôts de Romain, à ignorer.
