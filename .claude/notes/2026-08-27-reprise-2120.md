# Reprise (dernière mise à jour : 2026-08-28, 11 h 05)

## État
- **Chantier 1 fusionné dans `main`** (`eecbbea`) : `/erom-seo:audit` niveau 0, 27 vérifications, recette AC-1 à AC-7, correctifs post-revue R-1 à R-6.
- **Correctif apex/www fusionné le 28/08** (`a9dbba0`, `8d43a49`, fast-forward, non poussé) : un sitemap listé en apex sur un site en www vidait la collecte en silence (1 page sur 10). `sameSite`, `pageKey`, stats `sitemapUrls` dans le manifeste, avertissement stderr, consigne dans SKILL.md, et `--max-pages N` rend bien N pages quand la home est dans le sitemap. 84 tests verts.
- **Premier test réel** : commentchercherbonheur.org (site de Romain, code dans `/Users/recarnot/dev/chico-happiness`, Next.js 16). Rapport à 10 pages : `clients/commentchercherbonheur.org/seo/audits/2026-08-28-n0-2/report.md` (0 Critique, 5 Important, 3 Mineur, 2 Info ; les 9 pages ont le même title). Le `-3` est une collecte seule.
- Frictions parquées, pas de décision prise : IDX-04 ne dit rien du cas 307 (jugé Important par prudence, à préciser dans `checks/indexability.md`) ; la preuve « fichier + lignes » ne marche pas sur un HTML minifié sur une ligne (citer le champ `derived/` et un grep reproductible, le template pourrait le dire).
- Worktree `/Users/recarnot/dev/erom-agence-seo-fix` et branche `fix/collect-sitemap-host` : fusionnés, à supprimer quand Romain le dit (`git worktree remove`, `git branch -d`).
- Utilisation : depuis `clients/<domaine>/`, `claude --plugin-dir ../../plugin` puis `/erom-seo:audit https://<site>`. `source ~/.zshenv` avant pour PERF-01 (clé PageSpeed) ; `BING_WMT_API_KEY` existe aussi.

## Chantier 2 : exécuté, recette faite, en attente de la revue finale de Romain
- Spec : `docs/superpowers/specs/2026-08-28-erom-seo-strategy-design.md` (D9 à D15, AC-1 à AC-8). Plan : `docs/superpowers/plans/2026-08-28-erom-seo-chantier-2-strategy.md` (11 tâches).
- **Exécution terminée le 28/08 dans le worktree `/Users/recarnot/dev/erom-agence-seo-chantier-2`, branche `chantier-2-strategy`, HEAD `a6996a1`** (13 commits de code/doc + 1 commit de recette). Subagent-driven development, un implémenteur + un relecteur par tâche, revue adversariale du plan avant exécution (3 bugs trouvés dans du code jamais exécuté, corrigés avant de coder), revue finale de branche par Fable (2 Important trouvés à l'échelle branche, corrigés en une vague de fix + re-review). `bun test` (plugin) : 170 pass, 0 fail.
- **Recette** : `docs/superpowers/plans/2026-08-28-erom-seo-chantier-2-recette.md`, AC-1 à AC-8 tous courus (AC-1 par Romain, AC-5 seconde moitié après son feu vert explicite, le reste en autonomie sur `chico-happiness` et le plugin). Diff laissé non commité dans `chico-happiness` (`src/app/methode/`, correctif title/h1) : Romain le relit avec `git diff`.
- **Pas de fusion faite** (règle du plan) : la branche est prête, Romain relit `recette.md` et le diff final avant de fusionner dans `main`.
- Findings mineurs parqués (non bloquants) : voir la fin de `recette.md` (declaredLocal non testé, hôte tiers réécrit sans distinction au niveau 2, pas de test sur strategy-path absent, nb_checks non validé, jsonLdBlocks inutilisé en prod).
- **Incident opérationnel** : une clé Bing affichée en clair dans le chat pendant la recette (commande de vérification mal construite). Signalé à Romain sur le moment. Mémoire `erom-seo-secrets-env.md` mise à jour.
- À rejouer le 1er septembre : la sonde JSON Bing (`GetKeywordStats`, attendu 400 InvalidApiKey) après le retrait SOAP/POX du 31 août ; `keywords.ts` en dépend.

## Hors chantier
- Règle anti-fuite de secrets dans le garde : confiée par Romain à une autre session.
- `.claude/skills/plugin-release/` et `.claude/skillsTMP/` : dépôts de Romain, à ignorer.
